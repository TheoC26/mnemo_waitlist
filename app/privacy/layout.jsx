import React from "react";

export const metadata = {
  title: "Mnemo - Privacy",
  description: "Mnemo's privacy policy",
};

const Layout = ({ children }) => {
  return children;
};

export default Layout;
